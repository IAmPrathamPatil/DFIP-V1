# Local source data

This directory is for **local** Web Engage exports and related drops.

- Contents are gitignored, except this README.
- Do not commit client workbooks.
- The original supplied evidence files currently remain in the repository root so they are not moved or renamed. They are still gitignored via `*.xlsx`.
