# apps/worker

Planned home of a background job runner (**V2**).

P3 ingestion and P4 transformation are libraries in
`packages/core/dfip_core/ingest` and `packages/core/dfip_core/transform`. This
folder has no job runner, no scheduler, and no queue.
